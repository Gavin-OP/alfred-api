---
type: adr
status: proposed
---

- 状态：proposed
- 日期：YYYY-MM-DD
- 关联：<ADR / 设计文档链接，如 ../adr/0021.md 或 ../prd/xx.md>

# ADR-00xx：<决策标题>

> 本文件为 ADR 模板，严格镜像 `README.md` 的四章节格式。ADR 承载「怎么实现」的取舍与理由；
> Vision / 首页 / index 只以链接指向本 ADR，**不得内联**其结论。格式细节见 `../STYLE.md` §6。

## 背景
描述促使我们做此决策的力量、约束与上下文：哪些问题必须解决、有哪些已知事实与限制。引用 PRD / 设计文档 / 既有 ADR。

## 决策
一句话给出结论：我们决定采用 <方案 X>。
（如需，可在此给出关键接口 / 结构 / 命名的核心定义。）

## 理由与代价
- 选择 <方案 X> 的理由：<为何优于其他选项>
- 付出的代价 / 负面后果：<迁移成本、限制、需补偿的措施>
- 受影响的既有决策：<若有，列出被取代的 ADR 编号，并标注 superseded_by>

## 备选方案
- 方案 A：<简述> —— 未选原因：<缺点>
- 方案 B：<简述> —— 未选原因：<缺点>

> 规则：已采纳的 ADR 不得修改或删除；若决策被推翻，新建 ADR 标注 `superseded_by` 指向新决策，旧 ADR 状态改为 `superseded`。
